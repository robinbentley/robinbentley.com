{
  "name": "robinbentley.com",
  "version": "1.0.0",
  "description": "Personal website of Robin Bentley",
  "private": true,
  "scripts": {
    "dev": "concurrently -k \"gulp\" \"hugo server --buildDrafts\"",
    "build": "rm -rf public/ && hugo && gulp htmlmin",
    "preview": "yarn run build && http-server -p 1337",
    "deploy": "yarn run build && ./deploy.sh"
  },
  "dependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "concurrently": "^3.1.0",
    "del": "^2.2.2",
    "gulp": "^3.9.1",
    "gulp-cssnano": "^2.1.2",
    "gulp-htmlmin": "^3.0.0",
    "gulp-imagemin": "^3.1.1",
    "gulp-sass": "^2.3.2",
    "normalize.css": "^5.0.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
